[tool.ruff]
target-version = "py311"
line-length = 120
extend-exclude = [
  ".git",
  ".pytest_cache",
  "artifacts",
  "data",
]

[tool.ruff.lint]
select = [
  "E9",
  "F63",
  "F7",
  "F82",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
